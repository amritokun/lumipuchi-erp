{
  "name": "lumipuchi-erp",
  "private": true,
  "version": "1.0.0",
  "description": "Production-ready open-source ERP for Indian e-commerce sellers",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
